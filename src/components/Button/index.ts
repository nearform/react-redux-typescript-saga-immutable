import styled from 'styled-components';

const Button = styled.button`
  background: #27ae60;
  color: white;
  font-size: 16px;
  font-weight: 600;
  padding: 10px 25px;
  border-radius: 5px;
  border: none;
  cursor: pointer;
  display: table;
  margin: 0 auto;
`;

export default Button;
